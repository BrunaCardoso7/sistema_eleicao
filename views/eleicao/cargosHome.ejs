<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Cadastrar Chapas</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bulma@1.0.0/css/bulma.min.css"> 
    <link rel="stylesheet" href="/css/global.css">>
    <link rel="stylesheet" href="/css/chapahome.css">
</head>
<body>
    <header class="header">
        <div class="header-value">
            <i onclick="window.history.back()" class="fa-solid text-light fa-chevron-left button-left"></i>
            <div class="logo-conteiner">
                <h2>Vota Aqui</h2>
                <i class="fa-solid fa-puzzle-piece"></i>
            </div>
        </div>
    </header>   
    <main class="conteiner">
        <div class="">
            <div class="text-info-header">
                <h2 class="subtitle">Cargos Registrados</h2>
                <p>Cadastre os cargos dessa eleição</p>
            </div>
            <div class="table-responsive">
      
                    <% cargos.forEach(cargo => { %>
                        <ul class="table-header">
                            <li><%= cargo.Nome %></li>
                            <li>
                                <a href="" class="button button-secundary">Ver Detalhes</a>
                                <form action="/cargos/<%= cargo.CargoID %>/<%= eleicaoId %>?_method=DELETE" method="post">
                                    <button type="submit" class="button button is-danger is-light">Deletar</button>
                                </form>
                            </li>
                        </ul>
                    <% }); %>

            </div>
            <div class="text-info">
                <p>Cadastre agora mesmo um novo cargo.</p>
                <div class="conteiner-button">
                    <a href="/" class="button is-back">Voltar</a>
                    <a href="/cadastroCargo/<%= eleicaoId %>" class="button button-primary">Novo cargo</a>
                </div>
            </div>
        </div>
    </main>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.bundle.min.js" integrity="sha384-urZbA+l6S4GHevO9TmBndXeLl7/J8SRvJq6etK0VNXQdW0Vjrm++8eZFCsVZ7k4V" crossorigin="anonymous"></script>
</body>
</html>
